using System;

namespace Gryffindor.Contract.Utilities
{
   public static class RepositoryFactory
   {
      /*public static IRepository<object> GetRepository(string repositoryType)
      {
         IRepository<object> repo = null;
         switch (repositoryType)
         {
            case "Service": repo = new ServiceRepository();
               break;
            case "SQL": repo = new SQLRepository();
               break;
            default:
               throw new ArgumentException("Invalid Repository Type");
         }
         return repo;
      }*/
   }
}
